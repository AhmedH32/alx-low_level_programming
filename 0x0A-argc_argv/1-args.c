#include "main.h"

/**
 * main - entry point
 * @argc: number of inputs
 * @argv: pointer to array of pointers
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", *argv[0]);
	return (0);
}
