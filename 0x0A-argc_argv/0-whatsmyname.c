#include "main.h"

/**
 * main - entry point
 * @argc: number of inputs
 * @argv: pointer to an array of pointers ot strings
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
