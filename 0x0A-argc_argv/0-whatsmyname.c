#include "main.h"

/**
 * main - entry point
 * @argc: number of inputs
 * @argv: pointer to an array of pointers ot strings
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
