#include "main.h"

/**
 * strtow - seperates a string into a 2d array of different words
 * @str: the string itself
 * Return: a pointer to a pointer
 */
char **strtow(char *str)
{
	int len = 0;

	if (str == NULL || str == "")
		return (NULL);
	while (str[len])
		len++;




}
